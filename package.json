{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "WhatsApp Cloud API webhook handler for per-message pricing model",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
